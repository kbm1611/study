# 군검찰·군사법원 사법 시스템 알고리즘 스킬 정리

군검찰, 군사법원, 사법 행정 시스템을 만든다고 가정했을 때 필요한 알고리즘과 CS 스킬을 업무 흐름 중심으로 정리한 문서입니다.

이 문서는 법률 판단을 자동화하기 위한 자료가 아닙니다. 사건 기록, 증거 관리, 일정 관리, 권한 검증, 감사 로그, 검색, 배당 같은 업무 보조 시스템을 설계할 때 필요한 기술 관점의 정리입니다.

## 전체 시스템에서 중요한 원칙

- 법적 판단은 사람이 하고, 시스템은 기록·검색·검증·추적을 보조한다.
- 모든 중요 행위는 감사 로그로 남겨야 한다.
- 사건, 인물, 증거, 문서, 일정, 처분은 서로 연결된 데이터로 본다.
- 권한은 역할, 소속, 사건 담당 여부, 보안 등급을 함께 고려한다.
- 변경 이력은 나중에 재구성할 수 있어야 한다.
- 민감 정보는 최소 접근 원칙으로 다룬다.

## 업무 흐름별 필요한 알고리즘

| 업무 영역 | 필요한 알고리즘/자료구조 | 쓰임 |
| --- | --- | --- |
| 사건 접수 | 해시, 상태 머신, 중복 검사 | 사건 번호 생성, 접수 상태 관리, 중복 사건 탐지 |
| 사건 배당 | 우선순위 큐, 정렬, 가중치 계산 | 담당자 업무량, 전문 분야, 기피·회피 사유 반영 |
| 수사 기록 관리 | 트리, 그래프, 버전 관리 | 기록 구조화, 문서 관계 추적, 변경 이력 관리 |
| 증거 관리 | 해시, Merkle Tree, 체인 검증 | 증거 무결성, 보관 이력, 위변조 탐지 |
| 공판 일정 | interval scheduling, 충돌 검사 | 재판 일정 배정, 출석자 일정 충돌 확인 |
| 권한 관리 | RBAC, ABAC, 그래프 탐색 | 사용자별 열람·수정 가능 범위 판단 |
| 검색 | 인덱싱, 전문 검색, 랭킹 | 사건, 문서, 인물, 증거 빠른 조회 |
| 감사 로그 | append-only log, 해시 체인 | 누가 언제 무엇을 했는지 추적 |
| 통계·대시보드 | 집계, 슬라이딩 윈도우, 누적합 | 처리 기간, 사건 수, 지연 건수 분석 |
| 알림·기한 관리 | 우선순위 큐, 스케줄링 | 기일, 제출 마감, 검토 요청 알림 |

## 사건 접수: 중복 검사와 상태 머신

사건 접수 단계에서는 같은 사건이 중복 등록되지 않도록 빠르게 검사해야 합니다.

필요한 스킬:

- `HashMap`: 사건 번호, 피의자, 일시, 장소 등 주요 키 기반 조회
- 문자열 정규화: 이름, 군번, 부대명, 날짜 형식 통일
- 상태 머신: 접수, 검토중, 배당완료, 보완요청, 종결 같은 상태 제한

상태 전이는 아무 상태에서나 바뀌면 안 됩니다.

```java
enum CaseStatus {
    RECEIVED,
    REVIEWING,
    ASSIGNED,
    SUPPLEMENT_REQUESTED,
    CLOSED
}
```

예시 전이:

| 현재 상태 | 가능한 다음 상태 |
| --- | --- |
| `RECEIVED` | `REVIEWING`, `SUPPLEMENT_REQUESTED` |
| `REVIEWING` | `ASSIGNED`, `SUPPLEMENT_REQUESTED`, `CLOSED` |
| `ASSIGNED` | `CLOSED`, `SUPPLEMENT_REQUESTED` |
| `SUPPLEMENT_REQUESTED` | `REVIEWING`, `CLOSED` |
| `CLOSED` | 없음 |

## 사건 배당: 정렬과 우선순위 큐

사건 배당은 단순 랜덤보다 기준이 명확해야 합니다.

고려할 수 있는 점수:

- 담당자 현재 사건 수
- 사건 분야 경험
- 긴급도
- 이해충돌 여부
- 휴가, 파견, 교육 등 가용 상태
- 동일 사건 관련자와의 관계 제한

필요한 스킬:

- `PriorityQueue`: 가장 적합한 담당자부터 선택
- 정렬: 업무량, 전문성, 긴급도 기준 정렬
- 필터링: 배당 불가능한 담당자 제거
- 가중치 계산: 여러 기준을 점수화

```java
class AssigneeScore {
    String userId;
    int activeCaseCount;
    int specialtyScore;
    int conflictPenalty;
    int urgencyFitScore;

    int totalScore() {
        return activeCaseCount * 10
                - specialtyScore * 3
                + conflictPenalty
                - urgencyFitScore;
    }
}
```

낮은 점수가 더 적합하다고 정하면 우선순위 큐로 후보를 관리할 수 있습니다.

```java
PriorityQueue<AssigneeScore> pq = new PriorityQueue<>(
        Comparator.comparingInt(AssigneeScore::totalScore)
);
```

## 수사 기록 관리: 트리와 그래프

수사 기록은 단순 파일 목록이 아니라 구조를 가져야 합니다.

예시 구조:

- 사건
- 피의자
- 피해자
- 참고인
- 진술서
- 증거물
- 압수수색 기록
- 송치 의견
- 공판 기록

필요한 스킬:

- 트리: 문서 폴더 구조, 기록철 구조
- 그래프: 인물, 사건, 증거 사이의 관계
- BFS/DFS: 특정 인물과 연결된 증거 찾기
- 위상 정렬: 선행 절차가 필요한 업무 순서 관리

그래프로 보면 이런 질문을 처리하기 좋습니다.

- 이 증거는 어떤 사건과 연결되어 있는가?
- 이 인물은 어떤 사건들에 등장하는가?
- 특정 사건과 간접적으로 연결된 문서는 무엇인가?
- 같은 증거가 여러 사건에 연결되어 있는가?

```java
Map<String, List<String>> relationGraph = new HashMap<>();
```

## 증거 관리: 무결성 검증

증거 관리에서 가장 중요한 것은 위변조 방지와 이력 추적입니다.

필요한 스킬:

- 해시: 파일 내용이 바뀌었는지 확인
- 해시 체인: 보관·이관 이력의 순서 검증
- Merkle Tree: 여러 증거 파일 묶음의 무결성 검증
- append-only log: 기록 삭제보다 추가만 허용

증거 이벤트 예:

| 이벤트 | 설명 |
| --- | --- |
| `REGISTERED` | 증거 최초 등록 |
| `TRANSFERRED` | 담당자 또는 보관 위치 변경 |
| `VIEWED` | 열람 |
| `COPIED` | 사본 생성 |
| `SEALED` | 봉인 |
| `SUBMITTED` | 법원 제출 |

해시 체인의 기본 아이디어:

```text
currentHash = hash(previousHash + eventData + timestamp)
```

이전 해시가 바뀌면 이후 해시가 모두 달라지므로 변경 탐지가 쉬워집니다.

## 공판 일정: 구간 충돌 검사

공판, 조사, 출석, 제출 기한은 시간 구간으로 볼 수 있습니다.

필요한 스킬:

- interval overlap: 일정 충돌 검사
- priority queue: 가장 가까운 마감부터 처리
- 정렬: 시작 시간 기준 정렬
- sweep line: 여러 회의실·법정 사용량 계산

두 일정이 겹치는지 확인하는 기본 조건:

```java
static boolean isOverlapped(int startA, int endA, int startB, int endB) {
    return startA < endB && startB < endA;
}
```

법정 배정에서는 종료 시간이 가장 빠른 법정을 우선 재사용할 수 있습니다.

```java
PriorityQueue<Integer> courtEndTimes = new PriorityQueue<>();
```

## 권한 관리: RBAC와 ABAC

사법 시스템은 권한 관리가 핵심입니다.

기본 권한 모델:

- RBAC: 역할 기반 접근 제어
- ABAC: 속성 기반 접근 제어

RBAC 예:

| 역할 | 가능한 작업 |
| --- | --- |
| 수사관 | 담당 사건 기록 작성 |
| 군검사 | 사건 검토, 처분 의견 작성 |
| 재판부 | 공판 기록 열람, 판결문 관리 |
| 서기 | 일정·문서 행정 처리 |
| 관리자 | 사용자·권한 설정 |

ABAC에서 고려할 속성:

- 사용자의 소속
- 사용자의 계급 또는 직책
- 사건 담당 여부
- 사건 보안 등급
- 문서 공개 범위
- 현재 절차 단계

권한 판단은 단순 `role == ADMIN` 같은 방식으로 끝내면 위험합니다.

```java
class AccessContext {
    String userId;
    String role;
    String unitId;
    String caseId;
    boolean assignedToCase;
    int userClearanceLevel;
    int documentSecurityLevel;
}
```

## 검색: 인덱싱과 랭킹

사건이 많아질수록 단순 순회 검색은 느려집니다.

필요한 스킬:

- inverted index: 단어에서 문서 목록으로 연결
- Trie: 자동완성, 접두어 검색
- 정렬과 랭킹: 최신순, 관련도순, 중요도순
- 필터링: 사건 상태, 담당자, 날짜, 문서 종류

검색 대상:

- 사건 번호
- 관련자 이름
- 부대명
- 죄명 또는 사건 유형
- 문서 제목
- 증거 번호
- 주요 키워드

간단한 역색인 구조:

```java
Map<String, Set<String>> invertedIndex = new HashMap<>();
```

## 감사 로그: 변경 이력 추적

누가 언제 무엇을 했는지 남기는 것은 사법 시스템에서 매우 중요합니다.

필요한 스킬:

- append-only log
- 해시 체인
- 이벤트 소싱
- 시간순 정렬
- 변경 전후 diff 저장

감사 로그에 들어가면 좋은 정보:

- 사용자 ID
- 역할
- IP 또는 접속 위치
- 수행 작업
- 대상 사건 또는 문서
- 변경 전 값
- 변경 후 값
- 요청 시각
- 성공 여부

```java
class AuditLog {
    String userId;
    String action;
    String targetType;
    String targetId;
    String beforeValue;
    String afterValue;
    long timestamp;
    String previousHash;
    String currentHash;
}
```

## 통계와 대시보드: 집계 알고리즘

관리자는 사건 처리 흐름을 숫자로 확인해야 합니다.

필요한 스킬:

- 누적합: 기간별 사건 수 빠른 계산
- 슬라이딩 윈도우: 최근 7일, 30일 추세
- 그룹핑: 부서별, 사건 유형별, 상태별 집계
- 정렬: 지연 사건, 긴급 사건 우선 표시

예시 지표:

- 접수 사건 수
- 배당 대기 사건 수
- 평균 처리 기간
- 공판 예정 건수
- 기한 임박 문서 수
- 보완 요청 후 미처리 건수

## 알림과 기한 관리: 스케줄링

기한 누락은 업무 리스크가 크므로 시스템이 보조해야 합니다.

필요한 스킬:

- priority queue: 가장 가까운 기한 먼저 처리
- cron-like scheduling: 반복 알림
- 상태 머신: 알림 발송, 확인, 재알림, 종료
- 중복 방지: 같은 알림을 여러 번 보내지 않도록 idempotency key 사용

```java
class DeadlineTask {
    String taskId;
    String caseId;
    long dueAt;
    int priority;
}
```

```java
PriorityQueue<DeadlineTask> deadlines = new PriorityQueue<>(
        Comparator.comparingLong(task -> task.dueAt)
);
```

## 데이터 모델링에서 필요한 사고

사법 시스템은 관계가 복잡하므로 테이블이나 객체를 단순히 나열하면 나중에 힘들어집니다.

핵심 엔티티:

- 사건
- 사용자
- 역할
- 조직
- 인물
- 문서
- 증거
- 일정
- 처분
- 공판
- 감사 로그

관계 예:

- 사건은 여러 인물과 연결된다.
- 사건은 여러 증거와 연결된다.
- 증거는 여러 이력 이벤트를 가진다.
- 문서는 사건, 작성자, 검토자, 공개 범위를 가진다.
- 일정은 사건, 장소, 참석자와 연결된다.

## 꼭 익히면 좋은 알고리즘 체크리스트

| 우선순위 | 알고리즘/스킬 | 이유 |
| --- | --- | --- |
| 1 | 해시 | 사건, 사용자, 문서, 증거 빠른 조회 |
| 1 | 정렬 | 사건 우선순위, 일정, 검색 결과 정렬 |
| 1 | 상태 머신 | 사건 단계와 알림 상태 관리 |
| 1 | 권한 검사 | 민감 정보 접근 제어 |
| 1 | 감사 로그 | 추적성과 책임성 확보 |
| 2 | 그래프 탐색 | 사건·인물·증거 관계 분석 |
| 2 | 우선순위 큐 | 배당, 기한, 긴급 사건 처리 |
| 2 | 구간 충돌 검사 | 공판·조사 일정 관리 |
| 2 | 인덱싱 | 빠른 검색 |
| 3 | 위상 정렬 | 선행 절차가 있는 워크플로우 |
| 3 | Merkle Tree | 대량 증거 묶음 무결성 검증 |
| 3 | 이벤트 소싱 | 변경 이력 기반 상태 재구성 |

## 구현할 때 주의할 점

- 사건 처분이나 법적 결론을 알고리즘이 자동 결정하게 만들면 안 된다.
- 추천, 배당, 위험 알림은 사람이 검토할 수 있게 근거를 함께 보여줘야 한다.
- 삭제 기능은 신중해야 하며, 실제 삭제보다 비활성화와 감사 로그 보존을 우선 고려한다.
- 개인정보와 민감 정보는 검색 결과에서도 최소한만 보여준다.
- 권한 실패 로그도 남겨야 이상 접근을 탐지할 수 있다.
- 시스템 시간, 타임존, 마감일 계산 규칙을 명확히 해야 한다.
- 테스트 데이터는 실제 개인정보처럼 보이지 않게 만든다.

## 학습 순서 추천

1. 해시, 정렬, 큐, 우선순위 큐
2. 상태 머신과 권한 검사
3. 그래프 탐색과 관계 모델링
4. 일정 충돌 검사와 스케줄링
5. 검색 인덱싱
6. 감사 로그와 해시 체인
7. 이벤트 소싱과 변경 이력 재구성
8. 대시보드 집계와 통계 처리

## 프로젝트 아이디어

간단한 포트폴리오 프로젝트로 확장한다면 다음 순서가 좋습니다.

1. 사건 등록과 상태 변경
2. 담당자 배당
3. 문서와 증거 등록
4. 일정 관리
5. 권한별 조회 제한
6. 감사 로그 기록
7. 사건 검색
8. 처리 현황 대시보드

이 순서대로 만들면 알고리즘, 자료구조, 백엔드 설계, 보안 사고를 자연스럽게 함께 연습할 수 있습니다.

